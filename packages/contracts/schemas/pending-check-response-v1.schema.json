{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:hezo-link:contract:pending-check-response:v1",
  "title": "Hezo Link pending check response V1",
  "description": "Strict public wire structure for one Pending Check Response V1. It validates structure only and defines no endpoint, HTTP status, polling behavior, token issuance, entropy proof, TTL, retention, storage, or network behavior.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "status",
    "check_token",
    "retry_after_ms",
    "expires_at",
    "request_id"
  ],
  "properties": {
    "schema_version": {
      "type": "integer",
      "const": 1
    },
    "status": {
      "$ref": "urn:hezo-link:contract:check-response-status:v1",
      "const": "pending"
    },
    "check_token": {
      "$ref": "urn:hezo-link:contract:check-token:v1"
    },
    "retry_after_ms": {
      "type": "integer",
      "minimum": 1,
      "maximum": 900000,
      "description": "Bounded positive integer wire value only; it does not define or promise polling behavior."
    },
    "expires_at": {
      "$ref": "urn:hezo-link:contract:canonical-instant:v1"
    },
    "request_id": {
      "$ref": "urn:hezo-link:contract:request-id:v1"
    }
  }
}
